// src/generators/flutter/readme.ts
import { FRAMEWORK_DISPLAY_NAMES } from '../../constants/index.js';
export function generateFlutterReadme(config) {
    const arch = FRAMEWORK_DISPLAY_NAMES[config.architecture] ?? config.architecture;
    const sm = FRAMEWORK_DISPLAY_NAMES[config.stateManager] ?? config.stateManager;
    const featureList = [
        config.features.dio && '- **Dio** — HTTP client with interceptors',
        config.features.goRouter && '- **GoRouter** — Declarative routing',
        config.features.firebase && '- **Firebase** — Auth, Firestore, Storage',
        config.features.supabase && '- **Supabase** — Backend as a service',
        config.features.hive && '- **Hive** — Fast local storage',
        config.features.isar && '- **Isar** — High-performance local database',
        config.features.localization && '- **Localization** — Multi-language support',
        config.features.flavors && '- **Flavor system** — dev/staging/prod environments',
    ].filter(Boolean).join('\n');
    return `# ${config.name}

> Generated by [KeroDev CLI](https://kerodev.dev) — Enterprise Flutter Scaffolding

## 🏗 Architecture: ${arch}

This project implements **${arch}** with **${sm}** for state management.

## 📱 Tech Stack

| Layer | Technology |
|-------|-----------|
| Language | Dart 3.3+ |
| Framework | Flutter 3.19+ |
| Architecture | ${arch} |
| State Management | ${sm} |

## 🔌 Integrations

${featureList || '- Core Flutter packages only'}

## 📁 Project Structure

\`\`\`
lib/
├── core/
│   ├── constants/         # App-wide constants
│   ├── extensions/        # Dart extensions
│   ├── errors/            # Failure & exception types
│   ├── network/           # HTTP client & interceptors
│   ├── router/            # GoRouter configuration
│   ├── storage/           # Local storage abstraction
│   ├── theme/             # Material 3 theme
│   └── utils/             # Shared utilities
├── features/              # Feature modules
│   └── <feature>/
│       ├── data/          # Data layer
│       ├── domain/        # Business logic
│       └── presentation/  # UI layer
├── shared/
│   ├── widgets/           # Reusable widgets
│   └── models/            # Shared models
├── config/                # App configuration
├── app.dart               # Root app widget
└── main.dart              # Entry point
\`\`\`

## 🛠 Getting Started

\`\`\`bash
# Install dependencies
flutter pub get

# Generate code (freezed, riverpod, etc.)
dart run build_runner build --delete-conflicting-outputs

# Run the app
flutter run

# Run on specific device
flutter run -d chrome     # Web
flutter run -d android    # Android
flutter run -d ios        # iOS

# Analyze code
flutter analyze

# Run tests
flutter test

# Build for release
flutter build apk --release      # Android
flutter build ios --release      # iOS
flutter build web --release      # Web
\`\`\`

## 🧪 Testing

\`\`\`bash
# Unit tests
flutter test test/unit/

# Widget tests
flutter test test/widget/

# Integration tests
flutter test integration_test/

# With coverage
flutter test --coverage
genhtml coverage/lcov.info -o coverage/html
\`\`\`

${config.devops.ci !== 'none' ? `## 🔄 CI/CD

GitHub Actions workflows run automatically on push/PR:

- ✅ Dart formatting check
- ✅ Flutter analyze (strict mode)
- ✅ Unit & widget tests
- ✅ Android APK build
- ✅ Web build

` : ''}

## 🎨 Theming

The project uses **Material 3** with a custom design system defined in \`lib/core/theme/app_theme.dart\`.

Colors, typography, and component styles are all centralized for consistency.

## 📐 Adding Features

1. Create a directory under \`lib/features/<feature-name>/\`
2. Add \`data/\`, \`domain/\`, and \`presentation/\` sub-layers
3. Register providers/blocs in your DI setup
4. Add routes in \`app_router.dart\`

---

*Generated by [KeroDev CLI](https://kerodev.dev) — Built for engineers who ship.*
`;
}
//# sourceMappingURL=readme.js.map