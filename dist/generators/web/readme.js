// src/generators/web/readme.ts
import { FRAMEWORK_DISPLAY_NAMES } from '../../constants/index.js';
export function generateWebReadme(config) {
    const fw = FRAMEWORK_DISPLAY_NAMES[config.framework] ?? config.framework;
    const css = FRAMEWORK_DISPLAY_NAMES[config.css] ?? config.css;
    const ui = config.ui !== 'none' ? FRAMEWORK_DISPLAY_NAMES[config.ui] : null;
    const pm = config.packageManager;
    const devCmd = pm === 'npm' ? 'npm run dev' : `${pm} dev`;
    const buildCmd = pm === 'npm' ? 'npm run build' : `${pm} build`;
    const lintCmd = pm === 'npm' ? 'npm run lint' : `${pm} lint`;
    const srcNote = config.framework === 'next' ? (config.srcDir ? 'Using `src/` directory.' : 'Root directory (no `src/`).') : '';
    return `# ${config.name}

> Scaffolded with [KeroDev CLI](https://kerodev.dev) — Enterprise Project Scaffolding

## 🚀 Tech Stack

| Layer | Technology |
|-------|-----------|
| Framework | ${fw} |
| Language | ${config.language === 'ts' ? 'TypeScript' : 'JavaScript'} |
| CSS | ${css} |
${ui ? `| UI Library | ${ui} |\n` : ''}\
${config.stateManager !== 'none' ? `| State | ${config.stateManager} |\n` : ''}\
| Package Manager | ${pm} |

${srcNote ? `> **Note:** ${srcNote}\n` : ''}

## 📁 Project Structure

\`\`\`
${config.framework === 'next' && config.srcDir ? 'src/' : ''}\
├── app/               # App router pages & layouts
├── components/
│   ├── ui/            # Reusable UI primitives
│   └── shared/        # Shared feature components
├── features/          # Feature-based modules
├── hooks/             # Custom React hooks
├── services/          # API service layer
${config.stateManager !== 'none' ? '├── store/             # State management\n' : ''}\
├── styles/            # Global styles + Tailwind @theme
├── types/             # TypeScript types
├── constants/         # App-wide constants
├── config/            # App configuration
└── lib/               # Utilities & helpers
\`\`\`

## 🛠 Getting Started

\`\`\`bash
${pm === 'npm' ? 'npm install' : `${pm} install`}
${devCmd}
${buildCmd}
\`\`\`

## 📜 Scripts

| Command | Description |
|---------|-------------|
| \`${devCmd}\` | Start dev server |
| \`${buildCmd}\` | Production build |
${config.features.eslint ? `| \`${lintCmd}\` | Run ESLint |\n` : ''}\
${config.features.prettier ? `| \`${pm === 'npm' ? 'npm run format' : `${pm} format`}\` | Format with Prettier |\n` : ''}\
${config.language === 'ts' ? `| \`${pm === 'npm' ? 'npm run type-check' : `${pm} type-check`}\` | TypeScript type check |\n` : ''}

## 🌍 Environment Variables

\`\`\`bash
cp .env.example .env.local
\`\`\`

${config.css === 'tailwind' ? `## 🎨 Tailwind CSS v4

This project uses **Tailwind CSS v4** with CSS-first configuration.

> ⚠️ There is **no \`tailwind.config.js\`** — all configuration lives in the CSS file.

Customize your theme in \`${config.framework === 'next' ? (config.srcDir ? 'src/app/globals.css' : 'app/globals.css') : 'src/styles/globals.css'}\`:

\`\`\`css
@import "tailwindcss";

@theme {
  --color-primary: oklch(0.623 0.214 255.67);
  --font-sans: 'Inter', sans-serif;
  /* ... add your design tokens here */
}
\`\`\`

` : ''}
${config.devops.docker ? `## 🐳 Docker

\`\`\`bash
docker compose up --build
\`\`\`
` : ''}

---

*Generated by [KeroDev CLI](https://kerodev.dev)*
`;
}
//# sourceMappingURL=readme.js.map